---
layout: base.njk
---

<article>
    <div class="post-body">
        <h1>{{ title }}</h1>

        {% if image %}<img src="{{ image }}" alt="{{ title }}" widt="600">{% endif %}

        <div> 
            {{ content | safe }}
        </div>
    </div>
</article>